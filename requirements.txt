# ReviewForge Python Dependencies
#
# Production dependencies for the ReviewForge API server.

fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
httpx>=0.24.0
pyyaml>=6.0
python-multipart>=0.0.6

# Database
sqlalchemy>=2.0.0
alembic>=1.11.0
asyncpg>=0.28.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# BUG: Version range too broad — allows vulnerable versions
requests>=1.0.0

# BUG: Using deprecated package
# pyyaml is fine, but using yaml.load without SafeLoader is the issue

# BUG: Extra index URL — potential supply chain attack vector
--extra-index-url http://custom.pypi.com/simple

# Internal packages
reviewforge-core>=0.1.0
